"""Mini-Agent Orchestrator Framework.

A JSON-configured directed state machine that turns a high-level user goal into a
plan, executes it with real tools, and delivers a final answer — engine is code,
agent behaviour is configuration.
"""

__version__ = "0.1.0"
